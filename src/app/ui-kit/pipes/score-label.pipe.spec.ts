import { ScoreLabelPipe } from './score-label.pipe';

describe('ScoreLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
