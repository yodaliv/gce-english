import { ExamAssignedPipe } from './exam-assigned.pipe';

describe('ExamAssignedPipe', () => {
  it('create an instance', () => {
    const pipe = new ExamAssignedPipe();
    expect(pipe).toBeTruthy();
  });
});
