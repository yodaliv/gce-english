import { AnswerIconPipe } from './answer-icon.pipe';

describe('AnswerIconPipe', () => {
  it('create an instance', () => {
    const pipe = new AnswerIconPipe();
    expect(pipe).toBeTruthy();
  });
});
