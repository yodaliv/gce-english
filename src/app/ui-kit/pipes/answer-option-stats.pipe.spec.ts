import { AnswerOptionStatsPipe } from './answer-option-stats.pipe';

describe('AnswerOptionStatsPipe', () => {
  it('create an instance', () => {
    const pipe = new AnswerOptionStatsPipe();
    expect(pipe).toBeTruthy();
  });
});
